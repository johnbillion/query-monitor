import { GlobalUtils, GlobalUtilsOptions } from '@johnbillion/plugin-infrastructure/acceptance';

export class QueryMonitorGlobalUtils extends GlobalUtils {
	constructor( options: GlobalUtilsOptions ) {
		super( options );
	}
}
