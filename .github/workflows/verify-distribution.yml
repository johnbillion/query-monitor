# yaml-language-server: $schema=https://json.schemastore.org/github-workflow

name: Verify Plugin Distribution
on:
  schedule:
    # Once per hour
    #
    #        ┌───────────── minute           (0 - 59)
    #        │  ┌────────── hour             (0 - 23)
    #        │  │  ┌─────── day of the month (1 - 31)
    #        │  │  │  ┌──── month            (1 - 12 or JAN-DEC)
    #        │  │  │  │  ┌─ day of the week  (0 - 6 or SUN-SAT)
    #        │  │  │  │  │
    #        │  │  │  │  │
    #        │  │  │  │  │
    - cron: '0  *  *  *  *'
  push:
    branches:
      - 'develop'
    paths:
      - '.github/workflows/verify-distribution.yml'
  pull_request:
    branches:
      - '**'
    paths:
      - '.github/workflows/verify-distribution.yml'
  workflow_dispatch:

permissions: {}

jobs:
  verify:
    name: Verify Plugin Distribution
    uses: johnbillion/plugin-infrastructure/.github/workflows/reusable-verify-distribution.yml@verify-distribution
    with:
      plugin: query-monitor
      owner: johnbillion
